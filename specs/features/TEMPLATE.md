# Feature: <name>

## Goal
<one sentence: what the user can do after this feature ships>

## Endpoint(s)
<method + path — link to specs/api/<file>.md for request/response shapes>

## Ports used
<list the IPort interfaces this feature calls — business logic must go through these, never call a vendor directly>

## Acceptance criteria
- [ ] <behavioral, user-facing, testable>
- [ ] <error case: what happens when X is wrong>
- [ ] <security: tenant isolation, access check>

## Out of scope
- <explicitly excluded — prevents scope creep>
